# 制造智能技术课程设计学习笔记
> 笔记内容包含：Vibe Coding与AI辅助开发工具学习、拟选用Harness工具与大模型、Git原理与功能学习、选题调研（含市场调研），为课程设计前期学习产出。

## 1 Vibe Coding与AI辅助开发学习
### 1.1 Vibe Coding核心概念学习
Vibe Coding是借助大模型完成软件开发的开发范式，不是直接复制AI生成代码，而是以人为主导，AI作为辅助工具，通过规格驱动、提示词工程、上下文管理、人工审查完成项目开发。主要学习到下面几个核心要点：

1. **提示词工程**
任务拆解：把一个大系统拆分成小模块（数据库设计、后端接口、算法模块、前端页面），分批次交给AI实现；
约束说明：写清楚技术栈、输入输出格式、禁止项（如不要直接接入真实硬件，AGV做软件仿真）；
验收标准：每一段prompt明确该部分代码需要达到什么效果，方便校验AI输出是否合格。

2. **上下文工程**
项目规则文件：在项目中维护规则文档，告知AI项目整体架构、命名规范、目录结构；
让AI先读已有代码再修改：修改已有功能时，把相关代码片段提供给AI，保证代码风格统一，避免AI写出与现有代码冲突的内容。

3. **规格驱动开发流程**
需求文档 → AI制定开发计划 → 分步实现 → 逐步验收。
先写`选题说明.md`、`方案设计.md`规格文档，再交给AI按照规格实现代码，而不是直接让AI自由写项目。

4. **AI代码审查、测试与调试接管策略**
- 自动化测试：每完成一个模块，编写简单单元测试，验证功能逻辑；
- 人工审查：AI生成的每一段代码必须读懂，不能直接复制，答辩需要能够讲解任意AI生成代码逻辑；
- 调试策略：AI代码出错时，采取回滚版本、缩小改动范围、最小复现问题的策略，不直接让AI全盘重写代码。

### 1.2 课程设计中的使用规划
本项目全程采用Vibe Coding模式开发：
1. 先完成md规格文档（选题说明、方案设计），再交给AI生成代码；
2. 保存全部关键prompt会话日志，作为课程设计过程档案；
3. AI生成代码后人工阅读审查，配合简单单元测试验证；
4. 出现bug时，借助git回滚，定位最小问题片段再提示AI修改。

## 2 拟选用Harness工具与大模型
### 2.1 Harness（AI代码开发工具）
> Harness工具用于承载代码生成、代码编辑、代码调试，作为Vibe Coding的载体。

|工具名称|用途选择|选择理由|
|---|---|---|
|通义灵码|主要编码Harness|IDE插件形式，可以直接在VS Code内完成代码补全、代码生成、代码解释，便于一边写代码一边审查，支持读取本地项目文件，适配本项目Python+Vue3技术栈。|

备选Harness工具：DeepSeek‑Code，用于复杂算法模块（LSTM、蚁群算法）的调试与改写。

### 2.2 底层大模型（模型）
1. **Qwen3.8**：主要模型，负责业务代码生成、接口编写、md文档撰写、单元测试生成。通用性强，对FastAPI、Vue3、PyTorch算法代码支持较好。
2. **DeepSeek‑v4**：备选，用于复杂算法逻辑（蚁群AGV路径规划、LSTM时序预测）的代码排错、逻辑解释；当Qwen输出算法存在bug时切换使用。

> 使用约束：所有AI生成代码必须人工理解，关键prompt、AI出错案例会记录在过程档案中，报告中会做AI使用披露。

## 3 Git原理与功能学习
本课程设计要求全程Git版本控制，小步提交，保留完整开发历史，因此学习Git基础原理与实操功能。

### 3.1 Git核心原理
Git是分布式版本控制系统，每个本地仓库拥有完整版本快照，不需要依赖远端服务器。核心区域分为：
1. **工作区**：我们实际编辑看到的项目文件；
2. **暂存区(index)**：存放将要提交的变更，`git add`将工作区改动加入暂存区；
3. **本地仓库**：存储每一次commit快照，`git commit`将暂存区内容保存成本地版本；
4. **远程仓库**：云端仓库，`git push`推送本地版本；`git pull`拉取远程更新。

每一次commit生成唯一哈希id，可以回退到历史任意版本，适合Vibe Coding开发中AI改动出错时回滚代码。

### 3.2 本项目需要使用的Git核心功能
1. `git init`：初始化本地仓库；
2. `git add`：将修改文件加入暂存区；
3. `git commit -m "有意义提交说明"`：小步提交，每次完成小功能就提交，提交说明清晰描述改动内容，禁止无意义如“update”；
4. `git log`：查看提交历史，查看每一次版本哈希；
5. `git checkout / git restore`：回滚文件版本，AI生成代码出错时回退到上一个可用版本；
6. `git push`：推送到远程仓库，保存完整项目历史，作为课程设计交付物。

### 3.3 本项目Git使用规划
1. 初始化仓库，首批提交包含：`.md`文档（选题说明.md、方案设计.md、学习笔记.md）；
2. 后续开发遵循小步提交：数据库设计完成提交；后端接口完成提交；每个算法模块完成分别提交；前端页面每完成一个页面提交；
3. 提交说明写清楚本次改动内容；完整保留全部提交历史用于教师检查过程考核。

## 4 选题调研
### 4.1 选题方向筛选
课程设计要求必须为**制造/工业场景**，至少覆盖课程3个技术方向，完成B/S架构Demo，不能是日常生活类项目。
前期筛选备选题目：
1. 设备故障预警运维系统
2. 零部件图像质检系统
3. 车间生产调度系统
4. **工厂仓储物料消耗预测与AGV配送调度系统（最终选定）**

选择理由：
1. 属于制造工厂仓储物流工业业务场景；
2. 可以落地3个课程技术方向：LSTM时序预测、蚁群群智能优化算法、Apriori关联规则挖掘，算法都参与业务逻辑，不是装饰性展示；
3. B/S架构容易实现，不需要真实硬件，AGV调度采用软件仿真，适合课程Demo开发；
4. 可以构建完整业务闭环：出入库数据录入 → 消耗预测 → 库存预警 → 生成配送任务 → AGV调度仿真。

### 4.2 市场调研（原有调研内容摘要）
随着智能制造推进，制造企业仓储向智能化升级。现有商用WMS仓储系统与AGV调度系统大多相互独立，物料补货依赖人工经验，缺少基于历史领料数据的预测自动驱动配送任务的能力。商用一体化软件价格昂贵，二次开发难度高；学术仿真只有算法，缺少完整B/S业务系统。

痛点总结：
1. 物料补货依靠管理人员经验，容易缺料或者物料积压；
2. 库存系统和AGV调度割裂，需要人工手动下发配送任务；
3. 缺少轻量化原型系统，将时序预测、数据挖掘、群智能优化算法结合打通仓储业务闭环。

本项目定位轻量化B/S原型Demo，模拟工厂仓储业务，完成预测‑预警‑AGV调度全流程演示。

### 4.3 技术可行性调研
1. 技术栈FastAPI+Vue3+SQLite成熟，课程Demo开发难度可控；
2. LSTM、Apriori、蚁群算法均为课程所学技术，可以基于Python实现仿真，不需要企业真实数据，可以脚本生成仿真数据集；
3. AGV采用软件仿真，不需要真实硬件，满足课程演示要求。

### 4.4 风险分析
1. 风险：LSTM预测效果较差；应对：本项目重点为系统Demo，预测允许仿真效果，核心看系统架构与算法模块集成，不追求工业级别预测精度。
2. 风险：蚁群多AGV调度出现死锁；应对：简化仿真仓储地图，优先保证业务流程跑通。

## 5 前期学习小结
通过前期学习，掌握Vibe Coding开发模式，确定了编码使用的Harness工具与底层大模型；掌握Git分布式版本控制原理与项目提交规范；完成多候选选题调研，选定《工厂仓储物料消耗预测与AGV配送调度系统》；完成市场调研，明确业务痛点、可行性。
后续将按照方案设计文档，搭建开发环境，生成数据集，逐步完成后端、算法、前端开发，使用git小步提交，留存prompt会话日志作为过程档案。

## 参考文献
[1] 张智海. 制造智能技术基础[M].北京:清华大学出版社,2022.
[2] Vibe‑Coding 开发指南[EB/OL].GitHub,2025.
[3] Git官方文档：分布式版本控制系统原理
[4] 通义灵码 Harness工具文档