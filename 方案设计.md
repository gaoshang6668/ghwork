# 方案设计：工厂仓储物料消耗预测与AGV配送调度系统

## 1 功能需求分析
### 1.1 用户角色
仓库管理人员：进行物料台账维护、查看库存、查看消耗预测结果、查看AGV仿真配送任务，接收库存预警。

### 1.2 功能性需求
1. **物料基础管理模块**
- 物料信息增删改查：物料编码、物料名称、安全库存、库位、单位；
- 出入库记录录入，历史领料数据入库；
- 库存状态查询，库存低于安全库存产生预警标记。

2. **物料消耗预测模块（LSTM）**
- 读取历史领料时序数据；数据预处理、归一化；
- LSTM模型训练，输出未来N天物料消耗预测值；
- 根据预测消耗量结合安全库存，判定是否触发补货预警；
- 前端展示预测曲线、预警列表。

3. **物料关联挖掘模块（Apriori）**
- 对历史领料事务数据集做关联规则挖掘；
- 输出物料之间同时领用的关联规则；
- 在生成配送任务的时候，根据规则推荐配套物料一同配送。

4. **AGV智能配送调度模块（蚁群算法仿真）**
- 接收预警触发自动生成的配送任务；
- 蚁群算法完成仓储地图下AGV路径规划；
- 多任务分配，仿真AGV运行路径；
- 调度结果回写数据库，前端可视化展示AGV仿真路径、任务状态。

5. **前端可视化模块**
- 库存看板、消耗预测曲线图；
- 预警消息列表；
- AGV任务列表、仿真调度结果展示；
- 物料管理表单页面。

### 1.3 非功能性需求
1. 架构为B/S浏览器访问模式；
2. Demo系统响应满足演示使用；
3. 算法模块与业务代码解耦；
4. 使用Git进行版本控制，小步提交；
5. 所有业务流程可以完整走通，实现业务闭环。

## 2 方案论证
### 2.1 备选方案对比
|方案|方案A：全部使用商用WMS与AGV系统|方案B：MATLAB仿真算法|方案C：本项目B/S原型Demo方案|
|----|----|----|----|
|优点|功能成熟|算法验证方便|完整业务系统，包含前端、数据库、业务逻辑+算法，符合课程B/S架构要求|
|缺点|闭源、无法二次开发写课程代码；成本高，无法展示内部算法实现|没有网页前端，没有业务数据库，只是纯算法仿真，不满足课程完整架构要求|为课程演示原型，不对接真实物理AGV硬件，使用软件仿真|

**论证结论：选择方案C**。任务书要求实现B/S架构完整Demo（前端UI、后端服务、数据库、算法模块）。方案C可以同时落地3项制造智能技术，完成业务闭环，适合课程设计开发。

### 2.2 技术方向业务匹配论证
1. LSTM时序预测：直接利用历史领料数据预测物料消耗，输出结果用于库存预警，驱动配送任务生成；
2. Apriori关联规则挖掘：挖掘领料记录，实现配套物料推荐，优化配送任务；
3. 蚁群算法：对配送任务做AGV路径规划仿真；
> 三项算法全部参与业务逻辑计算，不是单纯展示算法代码。

## 3 方案规划
### 3.1 整体技术栈
- **前端**：Vue3 + Element Plus，实现网页UI、图表展示(ECharts画预测曲线)
- **后端**：Python FastAPI，提供Restful API接口，处理业务逻辑
- **数据库**：SQLite（轻量，课程demo不需要安装额外数据库服务）
- **算法模块**：
  - LSTM：PyTorch实现时序预测；
  - Apriori：python实现关联规则挖掘；
  - 蚁群算法：Python实现AGV路径规划仿真。
- **版本管理**：Git，本地仓库推送到远程仓库，每一次功能迭代提交commit。
- **AI开发模式**：Vibe Coding（提示词工程、规格驱动开发、人工审查AI生成代码）

### 3.2 需要配置外部资源
1. Python环境，PyTorch、fastapi、uvicorn、pandas、numpy、matplotlib、echarts相关依赖包；
2. Git环境；
3. 不需要购买硬件；AGV为软件仿真。

### 3.3 数据集来源
> 课程设计不要求企业真实数据
1. 主要数据集：Python脚本仿真生成制造车间物料领料时序数据集；字段：物料ID、领料时间、领用数量、领料工单编号、同时领用物料集合。
2. 仓储地图仿真数据：仓库库位坐标、起点、终点，模拟AGV运动栅格地图。

### 3.4 前端方案
页面规划：
1. 登录/首页看板：库存总览、预警提示；
2. 物料管理页：物料维护、出入库记录表单；
3. 消耗预测页面：历史数据、LSTM预测曲线图，预警列表；
4. 关联规则结果页面：物料关联挖掘结果展示；
5. AGV调度仿真页面：任务列表、路径仿真结果展示。

### 3.5 后端与算法方案
1. 业务层FastAPI：处理CRUD业务，接收前端请求，读写SQLite数据库；
2. 算法模块独立文件夹，和业务接口分离：
- lstm_predict.py：时序预测模型训练与预测接口；
- apriori_mining.py：关联规则挖掘；
- aco_agv.py：蚁群算法AGV路径规划仿真；
3. 业务逻辑流转：库存触发预警 → 调用关联规则得到配套物料 → 生成配送任务 → 调用蚁群算法计算路径 → 结果存入数据库返回前端。

### 3.6 系统架构简要描述
B/S架构，浏览器（前端Vue）→ FastAPI后端服务 → SQLite数据库 + 独立算法模块。

## 4 技术路线
1. 环境搭建，Git初始化仓库；完成Vibe Coding学习；
2. 数据集准备：仿真生成物料领料数据，数据清洗，导入数据库；
3. 后端开发：数据库表设计，物料管理相关API开发；
4. 算法模块开发1：LSTM物料消耗预测，调试预测输出；
5. 算法模块开发2：Apriori关联规则挖掘；
6. 算法模块开发3：蚁群算法AGV路径规划仿真；
7. 前端页面开发，对接后端全部API；
8. 模块集成联调：打通完整业务流程，从录入出入库数据到预测‑预警‑生成AGV任务‑仿真调度；
9. 简单自动化测试，修复bug；
10. 撰写报告，录制演示视频，准备答辩PPT。

## 5 计划安排（对应课程设计10天进度）
|阶段|时间|工作内容|
|---|---|---|
|阶段1|D1‑D2|Vibe coding学习，环境配置，Git仓库初始化，市场调研，完成选题说明.md，方案设计.md提交仓库|
|阶段2|D3‑D5|数据库表设计；数据集生成导入；后端基础接口开发；开始三个算法模块编码|
|阶段3|D6|完成全部算法模块调试；验证算法输出结果；|
|阶段4|D7‑D8|Vue前端页面开发；前后端对接，业务联调；跑通完整业务闭环；Git频繁小步提交；保存prompt日志|
|阶段5|D9|系统集成测试；撰写课程设计报告；录制3分钟演示视频；整理全部过程档案|
|阶段6|D10|整理全部交付物，准备答辩PPT，参加答辩|

## 6 参考文献
[1] 张智海. 制造智能技术基础[M].北京:清华大学出版社,2022.
[2] 杨杰.人工智能基础[M].北京:机械工业出版社,2025.
[3] 王健, 张磊.基于LSTM的工厂物料需求预测研究[J].制造业自动化,2023.
[4] 李刚.基于改进蚁群算法的多AGV仓储路径规划[J].计算机仿真,2024.
[5] 刘佳. Apriori算法在车间领料关联分析中的应用[J].工业控制计算机,2022.
[6] 王浩.Vibe‑Coding模式AI辅助软件开发实践[EB/OL].GitHub,2025.