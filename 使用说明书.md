```
# 工厂仓储物料消耗预测与 AGV 配送调度系统
## 使用说明书

## 1 项目简介
本项目为制造智能技术课程设计B/S演示原型，基于FastAPI + SQLite开发，实现车间物料出入库管理、仿真数据生成、ARIMA时序消耗预测、孤立森林出入库异常检测、PHM库存健康评估。系统预留扩展接口，后续可接入LSTM时序预测、Apriori物料关联推荐、蚁群算法AGV路径仿真模块。
> 本系统仅软件仿真，**不对接真实AGV硬件**。

## 2 环境要求
- Python版本：3.10 及以上
- 操作系统：Windows / MacOS
- 依赖包清单：`requirements.txt`

## 3 项目文件结构
```

ghwork/
├─ main.py                 # FastAPI 主服务，后端接口与前端页面
├─ models.py               # SQLAlchemy 数据库 ORM 模型，定义物料、出入库、预测、AGV 任务数据表
├─ init_data.py            # 初始化基础物料数据脚本，向数据库插入初始物料样本
├─ requirements.txt        # Python 项目依赖清单
├─ README.md               # 项目总说明文档
├─ 设计说明书.md           # 课程设计说明书
├─ 使用说明书.md           # 本文档，系统操作指引
├─ .gitignore              # Git 忽略配置
├─ data/                   # 数据集目录
│   ├── generate_dataset.py # 模拟数据集生成脚本
│   ├── data_preprocess.py  # 数据预处理脚本
│   └─ processed/           # 预处理后数据输出目录（运行脚本自动生成）
└─ docs/                   # 课程设计文档
├── 选题说明.md
└─ 方案设计.md

```
> gh.db 为SQLite本地数据库文件，不会上传至代码仓库，程序运行时自动生成。

## 4 部署运行步骤
> 操作前：打开终端，切换到项目根目录 `ghwork`
### 4.1 安装全部依赖库
```bash
pip install -r requirements.txt
```

### 4.2 初始化数据库与基础物料

```
python init_data.py
```

执行后自动创建数据表，并且插入 5 条基础物料数据。

### 4.3 启动 Web 后端服务

```
python main.py
```

服务启动成功后，在浏览器访问地址：

```
http://127.0.0.1:8000
```

## 5 系统功能操作指引

### 5.1 首页：物料管理与出入库操作

1. 页面展示所有物料的名称、当前库存、安全库存；
2. 支持手动执行**入库**、**出库**操作，填写数量提交，库存实时更新，并生成出入库记录；
3. 【一键模拟出入库数据】按钮：点击自动批量生成历史出库记录，快速构建时序数据集，用于 AI 预测。

> 
> ⚠️ 时序预测需要至少 6 条出库历史记录，样本不足时预测模块无法输出结果。

### 5.2 智能 AI 分析面板

进入 AI 分析页面，系统自动计算并展示每一项物料指标：

1. 未来 7 天物料预测消耗量（ARIMA 时序模型）
2. 系统自动计算建议补货数量
3. PHM 库存健康状态：正常 / 短缺风险 / 积压风险 / 数据不足

> 
> 状态颜色标识：绿色 = 正常，黄色 = 积压风险，红色 = 短缺风险，灰色 = 数据不足。

### 5.3 出入库异常记录查看页面

1. 展示全部出入库历史明细；
2. 孤立森林算法自动识别异常出入库操作；
3. 被判定为异常的记录在页面标红，便于快速发现异常领料行为。

## 6 数据集脚本使用（data 目录）

切换到 data 文件夹执行脚本

```
cd data
```

1. 生成原始仿真数据集

```
python generate_dataset.py
```

自动生成车间领料 CSV 仿真数据集。
2. 数据预处理

```
python data_preprocess.py
```

完成缺失值、异常值、重复记录清洗，标准化时间字段，生成 Apriori 算法所需工单事务集，输出到`data/processed/`目录。

## 7 预留扩展功能（暂未开发，方案设计阶段）

1. LSTM 深度学习时序预测：替换当前 ARIMA 模型，提升非线性消耗预测精度
2. Apriori 关联规则挖掘：挖掘工单物料配套关系，推荐物料合并配送
3. 蚁群算法 AGV 路径规划仿真：多任务 AGV 配送路径寻优

## 8 常见问题与故障排查

1. **访问页面报错，数据库表不存在**

> 
> 解决：先执行一次 `python init_data.py` 初始化数据表。

2. **AI 预测无结果，提示数据不足**

> 
> 解决：在首页点击【一键模拟出入库数据】，生成足够多的出库历史记录。

3. **pip 安装依赖失败**

> 
> 检查 Python 环境，建议使用 Python3.10 及以上版本。

4. 修改代码后页面不更新

> 
> 重启 main.py 服务，后端代码修改需要重启服务生效。

## 9 注意事项

1. gh.db 为本地数据库文件，无需提交到 GitHub，每次在新环境运行会自动重建；
2. 本系统为课程演示 Demo，仅用于课程设计演示；
3. AI 辅助开发，所有算法逻辑与业务代码均经过人工阅读与调试。